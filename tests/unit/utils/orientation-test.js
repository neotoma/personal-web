import orientation from 'personal-web/utils/orientation';
import { module, test } from 'qunit';

module('Unit | Utility | orientation');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = orientation();
  assert.ok(result);
});
