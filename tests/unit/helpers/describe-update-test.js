import { describeUpdate } from 'web/helpers/describe-update';
import { module, test } from 'qunit';

module('Unit | Helper | describe update');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = describeUpdate([42]);
  assert.ok(result);
});
