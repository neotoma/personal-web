import { toIsoString } from 'web/helpers/to-iso-string';
import { module, test } from 'qunit';

module('Unit | Helper | to iso string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toIsoString([42]);
  assert.ok(result);
});
