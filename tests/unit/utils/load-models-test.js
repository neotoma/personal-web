import loadModels from 'personal-web/utils/load-models';
import { module, test } from 'qunit';

module('Unit | Utility | load models');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = loadModels();
  assert.ok(result);
});
