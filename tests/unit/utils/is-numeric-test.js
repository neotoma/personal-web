import isNumeric from 'personal-web/utils/is-numeric';
import { module, test } from 'qunit';

module('Unit | Utility | is numeric');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isNumeric();
  assert.ok(result);
});
