import attribute from 'personal-web/utils/attribute';
import { module, test } from 'qunit';

module('Unit | Utility | attribute');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = attribute();
  assert.ok(result);
});
